<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="student">
	<select id="list" parameterType="chapter08.StudentVO"
		resultType="chapter08.StudentVO">
		SELECT *, (SELECT name FROM professor WHERE no = student.profno)
		profname FROM student
		<where>
			<if test="searchWord != null and searchWord != ''">
				<if test="searchType == 'all'"> id LIKE '%${searchWord}%' OR name LIKE
					'%${searchWord}%' </if>
				<if test="searchType != 'all'"> ${searchType} LIKE '%${searchWord}%' </if>
			</if>
		</where>
	</select>
</mapper>